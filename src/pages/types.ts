export interface ITowCars {
  tow: string;
  description: string;
}
export interface IInfo {
  infoName: string;
  description: string;
  secondDescription?: string;
}

export type TExamples = string[];
